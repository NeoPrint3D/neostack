---
import { PagesLogin } from "@/components/PagesLogin";
import Layout from "@/layouts/Layout.astro";

if (Astro.locals.auth) {
  return Astro.redirect("/dashboard");
}
---

<Layout>
  <PagesLogin client:load />
</Layout>
